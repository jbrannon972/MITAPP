rules_version = '2';
service cloud.firestore {
match /databases/{database}/documents {
match /hou_settings/secure_config {
allow write: if request.auth != null;
allow read: if false;
}
match /{document=**} {
  allow read, write: if request.auth != null;
}
}
}